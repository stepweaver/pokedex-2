import React from 'react';
import {
  BrowserRouter as Router,
  Route,
  Routes
} from 'react-router-dom';

const Components = () => {
  return (
    <Router>
      <Routes>
      </Routes>
    </Router>
  )
}

export default Components;